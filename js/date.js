document.addEventListener("DOMContentLoaded", function () {
  const dateText = "August 8, 2026";
  const el = document.getElementById("last-updated-date");
  if (el) {
    el.textContent = dateText;
  }
});
