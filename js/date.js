document.addEventListener("DOMContentLoaded", function () {
  const dateText = "June 09, 2026";
  const el = document.getElementById("last-updated-date");
  if (el) {
    el.textContent = dateText;
  }
});
