document.addEventListener("DOMContentLoaded", function () {
  const dateText = "May 7, 2026";
  const el = document.getElementById("last-updated-date");
  if (el) {
    el.textContent = dateText;
  }
});
