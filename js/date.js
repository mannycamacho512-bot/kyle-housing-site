document.addEventListener("DOMContentLoaded", function () {
  const dateText = "September 14, 2026";
  const el = document.getElementById("last-updated-date");
  if (el) {
    el.textContent = dateText;
  }
});
