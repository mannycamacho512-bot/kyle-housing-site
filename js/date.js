document.addEventListener("DOMContentLoaded", function () {
  const dateText = "May 6, 2026";
  const el = document.getElementById("last-updated-date");
  if (el) {
    el.textContent = dateText;
  }
});
