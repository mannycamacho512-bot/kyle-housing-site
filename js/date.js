document.addEventListener("DOMContentLoaded", function () {
  const dateText = "March 30, 2026";
  const el = document.getElementById("last-updated-date");
  if (el) {
    el.textContent = dateText;
  }
});
