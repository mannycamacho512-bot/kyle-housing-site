document.addEventListener("DOMContentLoaded", function () {
  const dateText = "June 24, 2026";
  const el = document.getElementById("last-updated-date");
  if (el) {
    el.textContent = dateText;
  }
});
