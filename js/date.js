document.addEventListener("DOMContentLoaded", function () {
  const dateText = "April 07, 2026";
  const el = document.getElementById("last-updated-date");
  if (el) {
    el.textContent = dateText;
  }
});
