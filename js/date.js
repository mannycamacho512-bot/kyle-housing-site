document.addEventListener("DOMContentLoaded", function () {
  const dateText = "August 26, 2026";
  const el = document.getElementById("last-updated-date");
  if (el) {
    el.textContent = dateText;
  }
});
